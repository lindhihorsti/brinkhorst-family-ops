# DO NOT COMMIT infra/.env
# Generate bcrypt hash:
#   docker run --rm caddy:2 caddy hash-password --plaintext 'YOUR_LONG_PASSWORD'
DENNIS_PWHASH='$2a$14$REPLACE_WITH_HASH'
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_MODEL_ACTIVITIES=gpt-5.2
